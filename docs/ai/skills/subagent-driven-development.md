# 子代理驱动开发

## 适用场景

只有当任务能够被拆分为边界清晰、写入范围明确、相互影响较小的子任务时，才使用并行或委派执行。

## 仓库要求

- 先有清晰计划，再拆分子任务；不要用并行执行代替规划。
- 各子任务的文件写入范围应尽量互不重叠，避免冲突。
- 集成子任务结果后，仍需按主任务标准执行验证与提交安全审查。
- 对三阶段模型、API Key 掩码、关键用户流程等高风险区域，不要随意并行拆散核心判断逻辑。

## 补充说明

并行提高的是吞吐，不替代设计、测试和复核。
