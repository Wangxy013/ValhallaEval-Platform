# 方案设计

## 适用场景

当需求引入新行为、涉及多个模块、存在多种合理实现路径，或用户流程会被明显改变时，先进行方案设计再进入实现。

## 仓库要求

- 先检查现有后端、前端、API 与文档结构，优先延续已有模式。
- 设计中必须明确受影响的模块、数据流、错误处理和验证方式。
- 若改动影响任务创建、校验、评估或结果分析，必须把三阶段模型拆分作为显式设计约束。
- 若改动涉及模型配置，必须把 API Key 掩码与编辑行为写入方案。

## 补充说明

方案设计文档应帮助后续实现，而不是变成与代码脱节的概念总结。
